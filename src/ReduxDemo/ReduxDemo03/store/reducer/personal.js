import * as TYPE from '../action-types'

/**
 *
 * @param {Object} state 原始redux管理的状态信息
 * @param {Object} action dispatch派发时传递的行为对象
 */

const personal = (state = { n: 0, m: 0 }, action) => {

  return state
}

export default personal
