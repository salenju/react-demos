import React from 'react'
import { Upload } from 'antd'
import styled from 'styled-components'

/**
 * @description
 * @param {*} props
 * @param {*} props
 * @param {*} props
 */

const UploadAndDrag03 = (props) => {

  return (
    <Wrapper className="ime-upload-drag">
     UploadAndDrag
    </Wrapper>
  )
}

const Wrapper = styled('div')`
`

export default UploadAndDrag03
